/**
 * Constantes compartidas del flujo de inducción (Academia + app móvil).
 * El curso se resuelve por `company_id` + categoría/tag en `courses` (multi-tenant).
 */
export const ONBOARDING_ACADEMY_COURSE_CATEGORY = 'Inducción Obligatoria';
